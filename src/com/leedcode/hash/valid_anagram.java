import java.util.HashMap;

/**
 * 1. sort 
 * 2. hash
 */
class Solution {
    private HashMapp<Characher, Integer> sMap;
    private HashMapp<Characher, Integer> tMap;
    
    public boolean isAnagram(String s, String t) {
        if (s.length() != t.length())
            return false;    

        sMap = new HashMap<Characher, Integer>();
        for (int i = 0; i < s.length(); i++) {
            if ()
            sMap.put(s.charAt(i), );
        }

        return 1;
    }
}